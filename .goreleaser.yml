project_name: myproject

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

    # Configuration for an application
    # Find the main entry point by priority order
    
    main: .   # Use the specified path if available
    
    ldflags:
      - -s -w
      - -X main.Version={{.Version}}
      - -X main.CommitSHA={{.Commit}}
      - -X main.BuildDate={{.Date}}
    binary: myproject


archives:
  - format: tar.gz
    name_template: "myproject_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      # LICENSE file is optional but recommended
      # - LICENSE
      # Documentation directory is optional
      # - docs/*

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - Merge pull request
      - Merge branch

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

# Generate Software Bill of Materials (SBOM) for all binaries
sboms:
  - artifacts: archive
    documents:
      - "${.ProjectName}_{{ .Version }}_{{ .Os }}_{{ .Arch }}_sbom.spdx.json"
    cmd: syft
    args: ["$artifact", "--file", "${.ProjectName}_{{ .Version }}_{{ .Os }}_{{ .Arch }}_sbom.spdx.json", "--output", "spdx-json"]

snapshot:
  name_template: "{{ incpatch .Version }}-next"

